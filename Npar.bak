#lang racket

(define(sumarpares n)
  (define max(* n 2))
  (unless(= (quotient max 2) 0) (set! max (+ 1 max)))
  (for ([i(in-range 2 max 2)])
    (newline)
    (display i))
  )


(begin(
      sumarpares 10
      ))